/** Appended only when the turn touches farming (see detect-context.ts). */
export const FARMING_BLOCK = `Farming detail: Farming is the Navy yield vault — an ERC-4626 pool on Sepolia. Depositing USDC mints navUSDC shares; the vault auto-rebalances the pooled USDC across yield venues (Compound III, Morpho) to earn variable yield. Withdrawing redeems navUSDC shares back for USDC. There is no lock-up — the user can withdraw any amount, or "all", at any time. The current value of a position (its USDC worth) can differ from the amount deposited as yield accrues. Check get_farming_summary for the current position (shares + current USDC value) before proposing a withdrawal whose size depends on the balance. To withdraw everything, pass the literal string "all" as the amount.`;
