import type { ToolCard } from './types';

export const getFarmingSummary: ToolCard = {
  name: 'get_farming_summary',
  description:
    "Use when the user asks about their farming position, yield, or earnings. Returns their Navy yield vault position — the navUSDC shares they hold and the current USDC value of those shares.",
};
